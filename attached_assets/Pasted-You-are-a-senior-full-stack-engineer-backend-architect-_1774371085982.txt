You are a senior full-stack engineer, backend architect, product engineer, and systems designer.

Your task is to help me rebuild a production-level SaaS platform from scratch inside this Replit project.

Context:
We previously developed and tested a working manuscript-processing workflow, but the Python backend/scripts were deleted by mistake and are no longer recoverable.
We still have the product knowledge, the use case, the lessons learned, and at least some frontend code already present in this codebase.
There may also be partial backend code, but I do not trust it yet.
So your first responsibility is to inspect what exists, identify reusable parts, and then rebuild the correct production-ready system.

Business use case:
This platform processes extremely large PDFs and converts them into cleaned, structured manuscript outputs suitable for Microsoft Word and later formatting/publishing.

Example input sizes:
- PDF around 2,900 pages
- PDF around 4,000+ pages
- PDF around 3,000 pages

The system must support multiple huge PDFs, one or more per project, and process them reliably in a resumable way.

Core pipeline we need to rebuild:
1. Upload large PDF(s)
2. Register a project
3. Detect file metadata and page count
4. Split PDF into safe chunks
5. Extract text chunk-by-chunk
6. Clean extraction artifacts
7. Preserve true content exactly
8. Remove AI/chat/user prompt junk and formatting noise
9. Generate cleaned DOCX manuscript parts
10. Separate appendix/reference material when detected
11. Persist manifests and processing state
12. Allow reruns for failed chunks only
13. Merge manuscript parts into final master DOCX
14. Optionally export final PDF
15. Provide a review/download UI for all artifacts

Critical preservation rule:
This platform is NOT a generative summarization tool.
It is a strict-preservation manuscript-processing system.

That means:
- do NOT summarize content
- do NOT paraphrase content
- do NOT creatively rewrite body text
- do NOT compress the manuscript
- do NOT shorten the text
- do NOT use LLM rewriting as the primary manuscript extraction strategy

The system should primarily:
- extract
- clean
- preserve
- structure
- export

The only acceptable transformations are:
- artifact removal
- structural normalization
- appendix/reference separation
- heading normalization if explicitly requested
- final merge/export operations

Known problems from prior iterations:
We already learned that:
- asking AI chat agents to process entire huge PDFs directly does not work reliably
- output limits cause truncation
- strict preservation must be chunk-based
- cleanup needs multiple passes
- residual artifacts often remain and need rule-based cleaning
- background processing and retryable chunk jobs are essential
- the user should not need terminal access or repeated AI prompting
- this must become a website workflow, not a manual script workflow

Goal:
Transform this project into a production-grade SaaS app where a user can do the full workflow through the web UI.

Required user experience:
A user should be able to:
1. Open the website
2. Create a new manuscript project
3. Upload one or more huge PDFs
4. Start processing
5. Watch project progress by phase and by chunk
6. Inspect outputs
7. Rerun failed chunks
8. Merge outputs
9. Download final DOCX/PDF artifacts

You must work step by step inside this codebase.

PHASE 1 — CODEBASE ANALYSIS
First inspect the current codebase and tell me:
- what frontend exists
- what backend exists
- what is usable
- what is broken or missing
- what architecture is currently implied
- what should be preserved
- what should be removed, commented out, or replaced

Then produce a concrete implementation plan.

PHASE 2 — SYSTEM ARCHITECTURE
Design a production-grade architecture for this SaaS.

The architecture should include:
- frontend app
- backend API
- background job/worker system
- persistent storage for uploads, chunks, outputs, manifests
- database schema for projects/jobs/chunks/artifacts
- file storage conventions
- merge/export subsystem

If the current stack already exists, adapt to it.
If key parts are missing, add them carefully.
Do not rewrite everything blindly.

PHASE 3 — BACKEND REBUILD
Rebuild the manuscript-processing backend.

The backend must include:

A. Project management
- create project
- attach one or more source PDFs
- track processing state

B. Upload handling
- large file upload support
- file metadata extraction
- page counting
- project storage structure

C. PDF chunking
- split huge PDFs into chunk ranges safely
- configurable chunk size
- preserve page order
- maintain page-to-chunk manifest

D. Extraction
- extract raw text from each chunk
- support reliable PDF text extraction libraries
- keep page markers internally for traceability if needed
- avoid OCR unless absolutely necessary

E. Cleaning pipeline
Implement rule-based cleanup stages such as:
- raw cleanup
- global artifact removal
- sentence-level artifact cleanup
- final micro-pass cleanup
- removal of conversation residue
- removal of “would you like”, “proceed to”, “next module”, “ready for next”, “copy into Word”, etc.
- removal of chat scaffolding
- removal of structural junk like PASTE START / PAGE BREAK markers if those are not real content
- preservation of formulas, tables, headings, disease content, menus, counseling scripts, reference tables

F. Appendix/reference separation
Detect and optionally separate:
- glossary
- conversion charts
- household measures
- appendix-like material
- reference tables

G. DOCX generation
Generate multiple cleaned DOCX manuscript parts from processed chunks.
Keep the order exact.

H. Merge preparation
Track ordered outputs and allow merge once chunk outputs are complete.

I. Retry/recovery
- rerun failed chunks only
- preserve completed chunks
- persistent manifests
- robust logging

PHASE 4 — FRONTEND INTEGRATION / REDESIGN
Use the existing frontend if possible, but redesign it around this use case.

Required pages/components:

1. Dashboard
- list projects
- status
- progress
- created date
- last updated
- outputs available

2. New Project / Upload
- upload one or more PDFs
- display file names, sizes, page counts
- create project

3. Project Detail Page
- project metadata
- source files
- processing phases
- start/resume processing
- chunk status table
- outputs list

4. Chunk Processing View
- chunk ID
- page range
- status
- retries
- logs/errors
- rerun button

5. Output Explorer
- cleaned DOCX parts
- appendix file
- manifests
- final merged document when available
- download actions

6. Merge / Export Page
- ordered file list
- merge action
- export DOCX
- export PDF if supported

7. Settings / Pipeline Config
- pages per chunk
- cleanup profile
- appendix extraction on/off
- merge options
- page marker behavior
- output naming convention

UI requirements:
- clean, simple, task-driven UI
- production-oriented UX
- strong status visibility
- error states
- loading states
- resume states
- large-file friendly
- no unnecessary complexity

PHASE 5 — DATA MODEL
Create a proper database/data model.

Suggested entities:
- User
- Project
- SourceDocument
- ProcessingJob
- Chunk
- ChunkOutput
- ProjectArtifact
- MergeJob
- FinalArtifact
- ProcessingManifest
- ErrorLog
- PipelineConfig

Include status enums such as:
- uploaded
- queued
- splitting
- extracting
- cleaning
- generating_docx
- completed
- failed
- merged
- exported

PHASE 6 — FILE/FOLDER STRATEGY
Design deterministic storage layout, for example:

/storage/projects/{projectId}/source/
/storage/projects/{projectId}/chunks/
/storage/projects/{projectId}/raw_text/
/storage/projects/{projectId}/cleaned_text/
/storage/projects/{projectId}/docx_parts/
/storage/projects/{projectId}/appendix/
/storage/projects/{projectId}/merged/
/storage/projects/{projectId}/manifest/

Use a clear convention and document it.

PHASE 7 — BACKGROUND JOBS
This system must not rely on the user keeping the browser open.

Implement:
- queue/worker model
- persistent job status
- resumable background processing
- chunk-level progress
- retry logic
- safe restart behavior

If the current stack supports a queue system, use it.
If not, implement the safest practical equivalent available in this Replit environment.

PHASE 8 — RELIABILITY / PRODUCTION READINESS
Build with production quality in mind:
- validation
- structured logging
- error boundaries
- retryable operations
- storage checks
- partial reruns
- manifest integrity
- deterministic chunk ordering
- protection against duplicate processing
- clear backend/frontend contract

PHASE 9 — MANUSCRIPT RULES
Hard rules for manuscript handling:
- preserve content exactly by default
- no summarization
- no paraphrasing
- no body-text rewriting
- remove only genuine artifacts
- preserve tables
- preserve formulas
- preserve headings
- preserve disease sections
- preserve counseling dialogue
- preserve sample menus
- preserve numeric values
- preserve chapter order

PHASE 10 — WHAT I EXPECT FROM YOU
You must not just give ideas.
You must actively inspect, plan, modify, add files, and implement.

In each major response, tell me:
- what files you inspected
- what files you changed
- what files you added
- what old logic you removed/commented out
- what now works
- what is still pending
- what I should test next

Execution order:
1. Analyze the codebase first
2. Propose implementation plan
3. Rebuild backend foundation
4. Add API endpoints
5. Wire frontend to backend
6. Add background processing
7. Add output/merge UI
8. Test end-to-end
9. Document local/dev usage

Do NOT skip analysis.
Do NOT jump into random rewrites.
Do NOT replace working parts unnecessarily.

Start now with:
STEP 1 — analyze the current codebase and provide:
- current architecture summary
- reusable parts
- missing parts
- risk areas
- proposed implementation plan
Then wait for my approval before making major structural changes.