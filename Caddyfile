{$DOMAIN} {
    encode zstd gzip
    reverse_proxy app:8000

    # Reasonable max upload size for exam PDFs.
    request_body {
        max_size 100MB
    }

    # Caddy auto-issues + renews Let's Encrypt SSL based on the {$DOMAIN} value.
}
